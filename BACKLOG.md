# Backlog

> 1 item = 1 session Claude (issue labellisée `claude` ou session Cloud) = 1 PR.
> Cocher + lien PR quand c'est mergé. `/dispatch` (claude-ops) lit ce fichier.

- [x] Retours UX mobile (lot 1) : filtre « En action », paramètres ⚙ (thème + token), retrait du + mobile — [PR #13](https://github.com/Thibaud888/fleetview/pull/13).
- [x] Retours UX mobile (lot 2) : codex v2 (détails, catégories, édition, suppression, filtre par projet), micro partout (demande, réponses, édition d'idée), option « cadrer d'abord » sur les demandes de changements.
- [x] Retours UX mobile (lot 3) : méta-filtres Tous/En action distincts, bouton « Idée » (même modale que Demande), codex sans barre d'ajout, dialogue avec navigation + agrandissement, cadrage sans étape GO (implémentation enchaînée), invite de permission micro (getUserMedia) + message d'aide Android.
- [x] Suivi & bootstrap (lot 4) : notifications ntfy cliquables (push sur question de Claude / PR prête, lien `?repo=` vers la vue projet), journal de run en direct (étapes Actions rafraîchies pendant la session), secret Claude au bootstrap (badge présence + « poser sans terminal »), compteur de quota API GitHub (topbar + paramètres, alerte sous 500).
- [x] Retours sur le lot 4 (lot 5) : notifications natives de l'appareil (API Notification via service worker, sans service tiers, clic → bon projet), correctif ntfy 405 (POST sur l'URL du sujet, métadonnées en query string), avertissement secret seulement si réellement manquant (404 confirmé, nom du secret en clair), dialogue en vrai plein écran (le fil sort de sa boîte et remplit l'appareil), journal du dernier run toujours visible dans la vue projet.

- [x] Service worker + cache hors-ligne — `sw.js` pré-cache la coquille (stale-while-revalidate), le dernier relevé est persisté et réaffiché avec un bandeau « hors ligne » ; l'API GitHub n'est jamais mise en cache (issue #8).
- [x] Icônes PWA PNG maskable (192/512) + apple-touch — installation propre sur Android : l'icône vitruvienne s'affiche masquée, pas une capture ; générées depuis `icon.svg` par `scripts/icons.mjs` (issue #7).
- [x] Journal de run en direct dans la vue projet — bloc « Journal du run » affiche les jobs/étapes Actions, rafraîchis toutes les ~4,5 s tant que le run tourne, avec lien vers les logs complets (les logs bruts ne sont pas lisibles côté client pour cause de CORS) — lot 4.
- [x] Secrets automatiques au bootstrap « Nouveau projet » — geste sans terminal : badge de présence du secret par projet + bouton « Poser / mettre à jour » (copie le nom, ouvre la page du secret) ; à la création d'un projet, l'onglet du secret s'ouvre automatiquement. Chiffrement libsodium volontairement écarté (règle zéro-dépendance) — lot 4.
- [x] Compteur de quota API GitHub — requêtes restantes lues sur les en-têtes `x-ratelimit-*`, affichées dans la topbar et les paramètres, alerte toast sous 500 — lot 4.
- [x] Notifications ntfy cliquables vers la vue projet — push sur nouveaux événements actionnables (question de Claude, PR prête), le lien `?repo=<id>` ouvre FleetView sur le bon repo ; sujet ntfy en localStorage, jamais commité — lot 4.
- [x] Passage du repo en public — fait : repo public + site en ligne sur [thibaud888.github.io/fleetview](https://thibaud888.github.io/fleetview/) (historique vérifié, aucun secret) ([PR #6](https://github.com/Thibaud888/fleetview/pull/6)).
- [x] Workflow Pages en pause tant que le repo est privé (garde `if` dans le stub, réactivation automatique au passage en public) — [PR #5](https://github.com/Thibaud888/fleetview/pull/5).
- [x] Bouton micro 🎙️ pour dicter les idées (Web Speech API) — [PR #10](https://github.com/Thibaud888/fleetview/pull/10).
- [x] Micro dans le champ titre du cadrage — bouton 🎙️ sur `#f-title`, même moteur générique `[data-mic]` que le détail, sans régression sur le micro du champ détail (issue #21, miroir de claude-ops#12).
- [x] Fiabiliser la boucle de dispatch — la vraie cause du `max_turns` (#19) était l'orphelin `npx serve` en CI : `verify.mjs` est passé sur un serveur HTTP natif (zéro téléchargement, arrêt propre), et `.mcp.json` (serveur MCP GitHub) + `mcp__github__*` dans l'allow-list ont été ajoutés — le tout dans [PR #20](https://github.com/Thibaud888/fleetview/pull/20). ⚠️ Le volet MCP reste **inerte en dispatch headless** tant que `fleet-kit/dispatch.yml` ne le câble pas (charger `.mcp.json`, ajouter `mcp__github__*` aux `--allowedTools`, exposer `GITHUB_TOKEN` au serveur MCP) ; le gain réel (fin des orphelins → plus de `max_turns`) est déjà acquis via `verify`. Câblage MCP côté fleet-kit = suivi optionnel.
- [x] Lanceur de session cloud — bouton « Session cloud » 🌩 sur chaque carte repo, chaque item du codex et la vue projet : compose un prompt de cadrage complet (repo, tâche + DoD, règles flotte), le copie au presse-papier (repli synchrone `execCommand` + filet API Clipboard) puis ouvre claude.ai/code ; si la copie échoue (mobile/http), une modale affiche le prompt à copier à la main. Aucune API publique ne permet de pré-remplir une session : le geste est copier → coller — [PR #31](https://github.com/Thibaud888/fleetview/pull/31).
- [x] Audit UX (lot 1) : boutons des cartes réparés (Examiner/Suivre/Répondre/Relancer déduisent le repo de la carte, chemins unifiés dans `openDetail()`), état des issues fiabilisé (âge depuis la dernière activité du fil, « en session » = runs `claude.yml` uniquement, ordre des branches revu pour le cadrage sans GO), liaison PR↔issue par frontière de mot, chroniques sans repos archivés — [PR #30](https://github.com/Thibaud888/fleetview/pull/30).
- [x] Audit UX (lot 2) : rate limit distingué d'un token invalide (bandeau + affichage conservé), messages d'erreur humains (`errMsg()` avec le message GitHub), brouillons de réponse préservés au relevé auto (focus + caret), modales fermées seulement au succès (plus de saisie perdue sur échec API), bandeau démo persistant — [PR #32](https://github.com/Thibaud888/fleetview/pull/32).
- [x] Audit UX (lot 3) : markdown rendu dans le dialogue et les corps de PR (mini-renderer maison, zéro dépendance), le fil s’ouvre sur la demande d’origine (corps de l’issue), meta theme-color synchronisée avec le thème, détails de PR limités aux repos suivis (quota), chroniques avec rétention par repo (6/repo, 30 total), scroll de l’atelier restauré au retour de la vue projet — [PR #33](https://github.com/Thibaud888/fleetview/pull/33).
- [x] Retrait du parcours cadrage par issue — la modale « Demande » remplace « 🪶 Cadrer puis lancer » par « 🌩 Session cloud » (compose le prompt et ouvre claude.ai/code, aucune issue créée) ; suppression de `cadrageBody()`, `changesCadrage()`, la branche `mode==="cadrage"` de `createRequest()`, le label `cadrage`, la case « cadrer d'abord » des demandes de changements et sa persistance ; `buildModel` ne distingue plus le label `cadrage` ; le 🚀 des idées ouvre désormais le parcours issue directe ; doc nettoyée (GUIDE.md, CLAUDE.md, MAP.md, README.md) — [PR #34](https://github.com/Thibaud888/fleetview/pull/34).
- [x] Lanceur mobile → app Claude — les boutons 🌩 sont désormais de vrais liens `<a href="claude.ai/code" target="_blank">` (au lieu de `window.open`) : sur mobile, un lien tapé est le seul déclencheur fiable des universal/app links, donc c'est la meilleure chance d'ouvrir l'app plutôt que le navigateur (sans garantie : dépend de la config de l'app). La copie du prompt reste dans le geste ; le parcours modale garde `window.open` — [PR #35](https://github.com/Thibaud888/fleetview/pull/35).
- [x] (P2) Canal cloud v2 : prompt court + ancrage issue — `composeCloudPrompt()` réduit à 2-3 lignes (1re ligne « <repo> — <tâche> », titre de session scannable dans claude.ai ; les règles de flotte ne sont plus recopiées, elles vivent dans le CLAUDE.md du repo équipé) ; le geste 🌩 sur une tâche crée d'abord une issue d'ancrage et le prompt demande « Closes #N » ; `buildModel` classe une issue `cloud` ouverte « en session » (la règle « muette après 2 h » ne s'y applique pas — un fil cloud est vide par construction, le dialogue est dans claude.ai) et l'anti-collision couvre les deux canaux (`loadAll` cherche `label:claude,cloud`). **Écart assumé au cadrage** : l'issue porte `cloud` **seul**, pas `claude`+`cloud` — la garde de `fleet-kit/dispatch.yml` déclenche sur `claude` et aurait lancé une session Actions en doublon de la session cloud. Le flux mobile passe par la modale « Copier et ouvrir » (2 taps) : créer l'issue est asynchrone, ce qui interdit la copie synchrone dans le tap initial ; le bouton de la modale est un vrai `<a>`, donc copie synchrone et universal link préservés. 🌩 depuis une carte (sans tâche) reste en 1 tap, sans ancrage. Vérification : `node scripts/verify.mjs` (il teste désormais le contrat du prompt — 1re ligne, brièveté, `Closes #N`) — [PR #53](https://github.com/Thibaud888/fleetview/pull/53).
- [x] Registre rafraîchissable depuis l'interface (suite audit UX, point 16) — un projet créé depuis FleetView restait invisible tant que `node scripts/fleet.mjs` n'avait pas tourné en local. Pré-requis côté claude-ops : workflow `fleet-refresh.yml` (`workflow_dispatch` + cron hebdo, avant `kit-propagation.yml`/`brief-hebdo.yml`) qui exécute `scripts/fleet.mjs` et committe `fleet/fleet.json` par API contents si le registre a changé — 0 token, pas de session — [claude-ops PR #35](https://github.com/Thibaud888/claude-ops/pull/35). Côté FleetView : `refreshRegistry()` déclenche `fleet-refresh.yml` (bouton « Rafraîchir le registre » en ⚙, ou automatiquement en fin de « Nouveau projet »), repère le run le plus récent (le `workflow_dispatch` ne renvoie pas d'id, 204 sans corps) et attend sa fin (même rythme que le journal, 4,5 s) avant de relire (`refresh()`) ; état affiché en ⚙ (`refreshRegistryStatus()`) et message explicite si le workflow claude-ops est absent (404, pré-requis pas encore posé). DoD tenu : créer un projet depuis FleetView le fait apparaître dans l'atelier sans terminal, une fois la PR claude-ops mergée et le run réel vérifié — [PR #57](https://github.com/Thibaud888/fleetview/pull/57).
- [x] Tous les fils de dialogue visibles dans la vue projet (suite audit UX, point 19) — un bloc Dialogue par issue `claude` ouverte (empilés), chacun avec son champ de réponse ; brouillons/focus préservés par fil au relevé auto, plein écran par fil, « Répondre » mène au bon fil ; second fil en démo sur `bulletins-viz` — [PR #41](https://github.com/Thibaud888/fleetview/pull/41).
- [x] Mobile : « À traiter » visible depuis tous les onglets (suite audit UX, point 21) — pastille rouge + compte sur l'onglet Atelier de la barre basse (visible depuis Codex/Tâches/Chroniques, un tap y mène), heure du dernier relevé lisible sur mobile (glissée dans la marge basse de la topbar), re-tap sur l'onglet Atelier actif → retour à la grille — [PR #45](https://github.com/Thibaud888/fleetview/pull/45).
- [x] Audit bugs & frictions (lot 6) : label `à-préciser` préservé à l'édition d'une idée, fil de dialogue visible même quand une PR référence l'issue, position de lecture des fils restaurée au relevé auto, notification marquée « vue » seulement après livraison effective, vue Tâches honnête quand le registre n'est pas chargé, lien « ← Revenir à l'atelier » depuis l'écran token, confirmation avant de perdre une saisie sur Échap/geste retour, toast si « ＋ Demande » avant le premier relevé, Ctrl/Cmd+Entrée pour envoyer une réponse, `theme-color` aligné et cache SW v4 — [PR #45](https://github.com/Thibaud888/fleetview/pull/45).
- [x] Vue « Tâches de la flotte » — lecture agrégée des BACKLOG.md de tous les repos actifs (à la demande : bouton ⟳ Lire / onglet mobile Tâches, ~1 requête par repo, jamais au relevé auto), items groupés par repo avec lanceurs ⚡ (pré-remplit la modale Demande, parcours issue directe) et 🌩 (session cloud avec tâche + DoD dans le prompt), badge 🔵 + ⚡ désactivé si une issue claude est déjà ouverte (anti-collision), 4e onglet mobile ; complément desktop : skill /backlog de claude-ops (mêmes fichiers, mêmes gestes).
- [x] Codex → backlog : bouton 🪶 « Cadrer » + états d'attente (volet interface du chantier G2 de claude-ops) — le codex est une salle d'attente, les BACKLOG.md l'unique stock de tâches : 🪶 par idée (codex + vue projet) déclenche le workflow unitaire `codex-cadrage.yml` de claude-ops, état « ⏳ à préciser » via le label `à-préciser`, lanceurs 🚀/🌩 retirés des idées (lancement depuis la vue Tâches après promotion), badge 📱 + filtre sur les items promus, GUIDE.md réécrit (§ codex). Tranché : lanceurs retirés. ⚠️ Promotion cross-repo en attente du secret `FLEET_GH_TOKEN` sur claude-ops — [PR #42](https://github.com/Thibaud888/fleetview/pull/42).
- [x] Chantier « moins lourdaud » (lot 7) : l'interface dit QUOI FAIRE au lieu d'afficher des états — boîte « Que faire ? » en tête de vue projet (une ligne + un bouton par action attendue), statut en clair sur chaque fil de dialogue (phase + explication), réponses en un clic aux questions de Claude (options ⭐ + « 👍 Fais au mieux », convention `**Options :**` partagée avec les sessions et le cadrage), codex v3 (question du cadrage affichée sur place avec réponse en un clic, groupes « ⏳ À toi de répondre » / « 🪶 Cadrage en route » / idées posées, questions remontées dans « À traiter » et en notification), rechargement qui laisse sur place (URL ?repo/?tab/?idea), prompt cloud enrichi (spec d'abord + contexte du repo, fini le « (à préciser) » vide), veilleur ntfy (cron `veilleur.yml` 15 min : notifications téléphone même app fermée, secrets `FLEET_GH_TOKEN`+`NTFY_TOPIC` à poser), passe de vocabulaire en français clair (modale, paramètres, états), GUIDE §7 Notifications. Volet claude-ops : cadrage v2 qui tranche par défaut (hypothèses notées), question unique à options, relance automatique sur réponse (commit 5e48f76 claude-ops) — [PR #47](https://github.com/Thibaud888/fleetview/pull/47). ✅ Veilleur **actif** depuis le 2026-07-19 : secrets posés, run de vérification vert ([issue #48](https://github.com/Thibaud888/fleetview/issues/48) fermée).
- [x] Durcissement post-revue (lot 8) — revue adversariale complète du chantier #47 (5 lentilles, 2 réfuteurs par finding, 14 confirmés, tous traités) : frontière de confiance bot (un tiers sur un repo public ne passe plus pour « Claude » et ne peut plus faire reposter son texte préfixé @claude), @mentions neutralisées dans les options relayées, « répondu » au codex par préfixe 🪶 (le PAT du cadrage cassait la détection par auteur — bug prouvé en prod), suppression d'idée sans cadrage fantôme, veilleur fiabilisé (fenêtre sur le run précédent terminé : plus de notifications en double ni de rejeu de panne ; vraie question seulement, PR liée exclue, PR sans CI couverte, pagination), asksQuestion resserré, parseOptions tolérant (option longue, rec fantôme, liste avant question), témoin d'état du veilleur dans ⚙, textes honnêtes (visible ≠ arrière-plan) — [PR #49](https://github.com/Thibaud888/fleetview/pull/49).

<!-- Lot 9 — retours d'usage mobile (11 points → 8 tâches). Recoupe les issues #58/#59/#60/#61. -->
- [x] (lot 9) Micro : mots répétés en cascade — au micro, chaque mot est réécrit plusieurs fois (« quand quand quand j'enregistre quand quand… », cf. issue #59). Rendre la dictée idempotente : reconstruire le texte à chaque événement depuis l'ensemble des résultats du run (au lieu d'accumuler depuis `resultIndex`, ce que le bug Android Chrome fait boucler), committer le final du run avant la relance sur `end`. DoD : dicter une phrase longue avec pauses ne produit aucune répétition ; `verify` passe. (issue #59) ✅ Cœur isolé en fonctions pures `micFinals()`/`micJoin()` (recomposent le run entier à chaque événement, jamais d'append depuis `resultIndex`), `committed` accumule uniquement au franchissement de `end`. Contrat verrouillé dans `scripts/verify.mjs` (`checkMic` : segment ré-émis, multi-segments, interim exclu, commit inter-runs). ⚠️ Confirmation réelle = un vrai micro (non automatisable ici).
- [x] (lot 9) « À traiter » mène droit à la bonne tâche — cliquer « Répondre » sur une entrée « À traiter » (ou « Que faire ? ») ouvre DIRECTEMENT le bon fil, défilé sur le DERNIER message (la question de Claude), champ de réponse focalisé — fini les 3 clics (« Répondre » → « Répondre ↓ » → « Dernier ») et la confusion « sur quelle tâche suis-je ? ». Le fil visé est mis en évidence ; les autres fils du repo ne noient plus celui qu'on vise. DoD : depuis l'accueil, un tap sur une question amène au message de Claude avec le champ prêt. (issue #61) ✅ Les entrées « À traiter » portent une cible (`data-thread`/`data-pr`) ; `openDetail(id, focus)` + `focusThread()`/`focusPr()` défilent le bloc sous la topbar (`scroll-margin-top`), le fil sur son dernier message, focus au champ (`preventScroll`), surbrillance `.flash`. `open-thread`/`open-pr` (« Que faire ? ») empruntent le même chemin. Vérifié dans Chromium (Playwright, mode démo) : tap « Répondre » → fil #21 focalisé et défilé au dernier message ; tap « Décider » → bloc PR amené dans la vue.
- [x] (lot 9) Fermer un fil de dialogue depuis FleetView — bouton « Fermer » sur chaque bloc Dialogue (commentaire de trace + fermeture `not_planned`, confirmation) pour classer une discussion sans suite sans passer par GitHub, quelle que soit la phase du fil. DoD : un fil peut se clore en un geste depuis la vue projet. (issue #60 ; intègre et supersède [PR #62](https://github.com/Thibaud888/fleetview/pull/62)) ✅ Bouton « ✕ Clore » sur chaque fil (`closeThread()`, même logique que `closeAnchor()`), confirmation en langage clair, plein écran renommé « ↙ Réduire » pour ne pas dupliquer « Fermer ». Vérifié dans Chromium (bouton présent, libellés distincts).
- [x] (lot 9) Résumés et demandes plus « haut niveau » — passe de vocabulaire sur les statuts, les hints, « Que faire ? » et « À traiter » : moins de jargon technique (issue #N, PR, checks, ancrage, dispatch, muette), formulations orientées « ce que tu as à faire ». DoD : un lecteur non technique comprend chaque ligne d'action sans terme d'implémentation. ✅ « muette/plantée » → « sans réponse / s'est arrêté », « Issue #N « … » » → « « … » », « checks » → « tests », « cron X.yml » → « automatisation « X » » (sans extension), « ancrage en plan » → « session cloud oubliée », en-tête de fil « Dialogue — issue #N » → « Dialogue #N — ». Conventions de protocole (commentaires postés, `**Options :**`) intactes ; mode démo aligné.
- [x] (lot 9) Notifications : app fermée + fin des doublons — une même alerte arrivait deux fois (notification de FleetView **et** ntfy). Rendre les canaux non redondants : sur l'appareil, native **ou** ntfy-in-app (jamais les deux), et clarifier le modèle à deux étages (natif = app ouverte, veilleur = app fermée) avec avertissement si le champ ntfy est rempli alors que le veilleur tourne (source du doublon). DoD : plus de double notification pour un même événement ; le rôle « app fermée » (veilleur) est explicite. (issue #58) ✅ `runPush()` : natif prioritaire, ntfy-in-app seulement en repli (fin du doublon interne) ; et **amorçage à l'ouverture** (`notifPrimed` → `seedNotified()` au 1er relevé) pour ne pas re-sonner ce que le veilleur a déjà poussé app fermée (le doublon « les deux en même temps quand j'ouvre »). État du veilleur en ⚙ signale « doublon probable » si le champ ntfy est rempli. Copies réglages + GUIDE §7 clarifiées.
- [x] (lot 9) S'assurer du bon repo à l'envoi — trop de demandes partent dans le mauvais repo. Étape de confirmation *user-friendly* avant l'envoi d'une issue directe / session cloud : le repo cible est affiché en clair à valider, et si le texte mentionne un autre repo connu que celui choisi, on le signale et on propose de basculer. DoD : impossible d'envoyer sans avoir vu (et pu corriger) le repo de destination. ✅ Barre de confirmation dans la modale : rappelle le titre + le projet de destination, et si `detectOtherRepo()` trouve un autre projet nommé dans le texte → alerte + bouton « → Plutôt « X » ». Le codex ne s'interrompt que sur détection (faible enjeu). Vérifié dans Chromium (mismatch+bascule, confirmation simple, reset sur édition).
- [x] (lot 9) Choix de repo lisible : juste le nom — les sélecteurs et cartes affichaient « atlas-quiz — cron-node · kit 1.4.0 » ; n'afficher que « atlas-quiz ». Retirer type + version de kit des endroits destinés à l'utilisateur (sélecteur de la modale, sous-titre des cartes, méta de la vue projet). DoD : plus de détail technique de type/kit dans le parcours de demande. ✅ `buildModel` : `type` ne porte plus « · kit X.Y.Z » (nettoyé sur cartes + méta) ; sélecteur de la modale réduit au nom du projet. Vérifié dans Chromium (aucune option avec type/kit).
- [x] (lot 9) Titre auto-rempli depuis la description — pouvoir ne remplir que la description : si le titre est laissé vide, il est résumé automatiquement depuis le début de la description (heuristique 1re phrase / quelques mots). Vaut pour la demande directe, la session cloud et l'idée codex. DoD : une demande sans titre saisi part avec un titre lisible dérivé de la description. ✅ `summarizeTitle()` (pure, testée `checkTitle`) : 1re phrase courte ou amorce coupée sur une frontière de mot, capitalisée ; champ titre passé optionnel (`required` retiré) ; le titre dérivé est rappelé dans la confirmation d'envoi. Vérifié dans Chromium.

- [x] (P2) Augmenter le délai de silence avant fermeture du micro — le délai actuel est trop court, ce qui ferme le micro pendant les pauses naturelles; augmenter ce délai (hypothèses: ~5-10s) pour permettre des pauses longues. DoD: pauses > 5s sans fermeture automatique, fermeture possible manuellement. — hypothèses: délai d'inactivité 5-10s, fermeture manuelle par reclic. 📱 ✅ 2026-07-20 — **il n'y avait aucun délai à augmenter** : `initMic()` posait `continuous=false`, donc c'était l'endpointing natif du navigateur qui coupait à la première pause (~1-2 s). Le délai a donc été *créé*, pas rallongé : `continuous=true`, constante `SILENCE_MS=8000`, timer armé au démarrage et **ré-armé à chaque mot reconnu** (il ne court que sur du silence réel), plus une relance automatique sur l'événement `end` — Chrome termine la reconnaissance de lui-même même en `continuous`, c'était la vraie cause des coupures. L'arrêt manuel (2e appui) reste prioritaire via un drapeau `wantStop`, qui coupe aussi la relance sur erreur définitive pour éviter toute boucle. ⚠️ **Vérifié partiellement** : le chemin « micro refusé » a été testé dans le navigateur (aucune boucle de relance, bouton éteint proprement), mais la tenue réelle sur une pause > 5 s **exige un vrai micro** et n'est pas automatisable ici — à confirmer d'un geste : ouvrir « ＋ Demande », dicter, se taire 6 s, vérifier que le micro tient, puis re-cliquer pour fermer. — [PR #55](https://github.com/Thibaud888/fleetview/pull/55).
- [x] (P3) Remonter les dispatchs en rade dans le veilleur — `dispatch_en_rade` (claude-ops `scripts/brief-rade.mjs`, PR #22) détecte les issues `claude` sans session et les PR de session jamais mergées, mais seul le brief hebdo du lundi le lisait : un item en rade le mardi attendait six jours. Nouveau `scripts/rade.mjs` (miroir volontaire de `brief-rade.mjs` : `synthetiseChecks()`, `estPrDeSession()`, `seuilFranchiDans()`), branché dans `veilleur.mjs` (cron 15 min, app fermée) : issue `claude` sans PR liée ni session Actions active (< 3h) au-delà d'1h → « dispatch en rade » ; PR de session (checks verts, ou repo sans CI) non draft toujours ouverte au-delà d'1h (verts) / 12h (sans CI) → « PR en rade ». Le veilleur est SANS ÉTAT : `seuilFranchiDans()` ne notifie qu'AU FRANCHISSEMENT du seuil (une fois), pas à chaque cron tant que l'item reste en rade. Contrat verrouillé dans `scripts/verify.mjs` (`checkRade`). DoD : un dispatch en rade est remonté en ≤ 15 min, sans attendre le lundi — [PR #57](https://github.com/Thibaud888/fleetview/pull/57).
- [x] Prompt de session cloud éclaté : le développé de la tâche disparaissait — `loadTasks()` découpait chaque item du BACKLOG.md sur le seul séparateur `" — "` (cadratin espacé) ; à défaut, le titre était tronqué à 160 caractères EN PLEIN MOT et le développé (DoD, contexte) purement jeté, d'où un prompt copié réduit au décor (« lis MAP.md… ») sans la tâche. Découpage extrait en fonction pure `parseBacklog()` : agrège les sous-lignes indentées (développé multi-ligne), coupe titre/développé au 1er tiret cadratin OU demi-cadratin, et SANS séparateur ne tronque plus le corps (titre scannable + corps entier en développé) — la version cadrée atterrit toujours dans le prompt. Marqueur 📱 (promue du codex) détecté et retiré du texte. Contrat verrouillé dans `scripts/verify.mjs` (`checkParseBacklog` : tiret, tâche longue sans tiret, multi-ligne, 📱, item coché ignoré).
- [x] (P3) Sessions cloud abandonnées : rien ne les détecte — trou ouvert par le canal cloud v2 ([PR #53](https://github.com/Thibaud888/fleetview/pull/53)). Une issue `cloud` est délibérément exemptée de la règle « muette après 2 h » (son fil est vide par construction, le dialogue vit dans claude.ai) : si tu ouvres une session 🌩 puis abandonnes sans ouvrir de PR, l'issue reste ouverte **indéfiniment** — le repo affiche « en session » pour toujours et l'anti-collision bloque ⚡ dessus. Le veilleur ne peut pas aider : il cherche `label:claude` et, même élargi, un fil cloud n'émet aucun signal. Piste : seuil long et distinct (p. ex. « ouverte depuis > 3 jours sans PR liée » → « à débloquer » avec bouton « Clore l'ancrage »), à ne pas confondre avec le seuil de 2 h du canal Actions. DoD : une issue d'ancrage laissée en plan finit par remonter, et se ferme en un geste depuis FleetView. ✅ 2026-07-20 — piste suivie telle quelle : constante `CLOUD_ABANDON_D = 3` (jours, mesurés depuis la **création**, bien distincte des 2 h du canal Actions), nouvelle branche dans `buildModel()` → état `crit` « ancrage en plan », remonté dans les lignes du repo, dans « À traiter » et dans la boîte « Que faire ? », avec bouton **« Clore l'ancrage »** (`close-anchor` → `closeAnchor()` : commentaire de trace puis fermeture en `not_planned`). Vérification exécutée sur la **vraie** `buildModel` (internes exposés temporairement le temps du test, hook retiré depuis) : 1 j et 2,9 j → `info` sans bouton ; 3,1 j et 9 j → `crit` + `close-anchor` ; **9 j avec PR liée → `warn` « PR ouverte »**, la non-régression qui comptait (une vieille session productive n'est pas signalée à tort). — [PR #55](https://github.com/Thibaud888/fleetview/pull/55).
- [x] Micro : les mots se répètent ENCORE après le lot 9 — retour d'usage du 2026-07-25, sur la case « détails » d'une demande. Le lot 9 avait rendu l'écriture idempotente **à l'intérieur d'un run** (`micFinals()` recompose au lieu d'accumuler), mais laissait le second étage ouvert : `continuous` ne survit pas aux coupures du moteur, on relance donc sur `end` (délai de silence, [PR #55](https://github.com/Thibaud888/fleetview/pull/55)) — et le moteur **rejoue dans le nouveau run ce qu'il venait de finaliser dans le précédent**, texte que `committed` porte déjà, donc réécrit derrière : répétition à chaque pause. Correctif : `micDedup()`, 3e fonction pure, retire du run la reprise **exacte** (comparaison insensible à la casse et à la ponctuation) de tout ou de la fin de l'acquis, en opposant deux références (toute la dictée, et le seul run précédent). Garde-fou anti-zèle : une reprise d'**un seul mot** n'est retirée que si elle constitue à elle seule la référence entière — « … le micro » puis « micro qui répète » reste intact. `checkMic` étendu (rejeu intégral, partiel, casse/ponctuation, non-rejeu, anti-zèle, boucle complète run → coupure → run) et **test de mutation fait** : `micDedup` neutralisée ⇒ `verify` échoue. Cache du service worker bumpé v7 → v8 pour que le téléphone prenne la correction dès le premier chargement. ⚠️ Confirmation réelle = un vrai micro (non automatisable ici).
- [x] Micro : la dictée se recollait derrière elle-même — 3e passe, sur test réel du 2026-07-25 (« ceci est un test du micro pour voir s'il y a des répétitions » → « micro micro micro pourra micro pourra s'il micro pourra s'il y… »). Ce texte est la signature d'un mécanisme précis : à chaque événement, la dictée ENTIÈRE était recollée derrière elle-même. Racine trouvée : `continuous` ne survit pas aux coupures du moteur, on relance donc — mais on relançait **la même instance** de reconnaissance, et sur Android `ev.results` n'y repart pas de zéro. Le nouveau run re-livrait tout le texte déjà dit, qu'on venait justement de committer. Les deux correctifs précédents (recomposition intra-run du lot 9, anti-rejeu textuel `micDedup` de [PR #65](https://github.com/Thibaud888/fleetview/pull/65)) rattrapaient le cas où le rejeu est identique au mot près, mais le moteur **révise** en cours de route (« du micro » → « de micro ») et la reprise passait au travers. Correctif structurel : **un run = une instance neuve** (`startRun()`), une relance ne peut donc plus porter les résultats de la précédente ; les deux étages textuels restent en filet. Verrou : `checkMicRuns()` dans `scripts/verify.mjs` joue l'IIFE `initMic` complète sur des stubs DOM avec un moteur factice qui reproduit le comportement Android (results conservés + segment révisé) — le code déployé échoue ce test, le nouveau passe. Ajouté au passage, parce que le diagnostic a coûté deux allers-retours : **témoin de version + bouton « Forcer la mise à jour » dans ⚙** (vide les caches, désinscrit le service worker, recharge), pour ne plus confondre « le correctif ne marche pas » et « l'app sert encore son cache ». Cache coquille v8 → v9.
- [x] Dictée par transcription (Whisper) — le 🎙 enregistre puis envoie l'audio, au lieu d'écouter en direct. Décidé le 2026-07-26 après un 3e échec du moteur du navigateur sur téléphone : trois correctifs (#63, #65, #67) n'ont pas suffi, parce que la reconnaissance en direct impose des relances et que le moteur Android rejoue et révise ce qu'il a déjà rendu. **Un enregistrement = un texte, une fois** : la famille de bugs disparaît par construction. Clé de transcription posée dans ⚙ (`localStorage`, comme le token GitHub, jamais commitée — `verify` refuse toute clé dans le dépôt) ; sans clé, repli sur la reconnaissance du navigateur, inchangée. Modèle `gpt-4o-transcribe`, langue forcée `fr`, conteneur choisi selon l'appareil (webm/opus sur Android, mp4 sur Safari) car l'API se fie à l'extension envoyée ; coupure automatique à 3 min si le micro est oublié ; le texte s'ajoute au champ. Vérification : `checkStt` (fonctions pures + aucune clé commitée) et `checkSttFlux` (parcours complet sur stubs : appui → enregistrement → 2e appui → envoi → texte dans le champ, avec URL, clé, modèle, langue et nom de fichier contrôlés), les deux prouvés par mutation. Coût ~0,006 $/min (~1 €/mois à 5 min/jour).
- [x] Savoir quelle version tourne, et pouvoir la mettre à jour — le diagnostic du micro a coûté deux allers-retours faute de le savoir, et « Forcer la mise à jour » (v1, [PR #67](https://github.com/Thibaud888/fleetview/pull/67)) ne suffisait pas : vider les caches du service worker ne touche pas au **cache HTTP du navigateur**, donc le rechargement resservait l'ancien `app.js`. Corrigé : le bouton relit maintenant la coquille en réseau (`fetch(…, {cache:"reload"})`, sur les MÊMES URL — une query `?maj=…` aurait laissé l'ancienne entrée intacte) avant de recharger. Et le témoin de version affiche désormais une marque **portée par le code lui-même** (constante `BUILD`), pas seulement le nom du cache : si elle ne change pas après mise à jour, c'est le fichier qui est vieux, pas le correctif qui rate. Cache coquille v9 → v10.
- [x] Confirmer que dicter marche enfin, en vrai — tout est livré et déployé ([PR #68](https://github.com/Thibaud888/fleetview/pull/68)), mais rien n'a encore été prononcé dans un vrai micro : aucun test automatique ne peut le faire. Geste attendu : ⚙ → **Forcer la mise à jour**, vérifier que la ligne « code : 2026-07-26 · dictée par transcription » s'affiche, poser une clé de transcription (⚙ → Dictée vocale), puis dicter « ceci est un test du micro pour voir s'il y a des répétitions » dans la case détails d'une demande. DoD : le texte arrive une fois, entier, sans mot répété. **Si ça échoue encore** : ne pas ajouter un 5e correctif à l'aveugle — trancher entre (a) diagnostiquer côté transcription (l'audio envoyé est-il bon ? le champ reçoit-il deux fois ?) et (b) retirer le bouton 🎙 de FleetView et s'en remettre à la dictée du clavier, plutôt que d'entretenir un mécanisme qui ne tient pas. Contexte complet des quatre passes : les items « Micro » ci-dessus, et [PR #63](https://github.com/Thibaud888/fleetview/pull/63)/[#65](https://github.com/Thibaud888/fleetview/pull/65)/[#67](https://github.com/Thibaud888/fleetview/pull/67)/[#68](https://github.com/Thibaud888/fleetview/pull/68). ✅ 2026-07-26 — confirmé par Thibaud sur son téléphone : la dictée marche, le texte arrive une fois, entier, sans répétition. Quatre passes auront été nécessaires ; ce qui a réglé le problème, c est de changer de moteur (enregistrer puis transcrire) plutôt que de continuer à rattraper la reconnaissance en direct.
